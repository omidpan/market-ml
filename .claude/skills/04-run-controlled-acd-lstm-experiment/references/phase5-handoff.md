# Phase 5 Handoff

Do not start Phase 5 until Phase 4 is complete.

Potential next experiments:

1. other OR/ATR configurations;
2. other regime policies;
3. feature-layer ablations;
4. correlation/redundancy analysis;
5. consensus features;
6. later walk-forward evaluation;
7. final sealed TEST evaluation after experiment selection is frozen.

For every future computational phase:

- create persistent Python entry points during that same phase;
- create corresponding Concourse CI YAML during that same phase;
- Claude validates syntax/wiring only unless explicitly asked to execute more;
- the user can later execute Concourse independently.
