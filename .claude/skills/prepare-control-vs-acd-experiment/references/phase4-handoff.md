# Phase 4 Handoff

Phase 4 may begin only after Phase 3 proves paired-universe identity.

The first training comparison should keep the current LSTM baseline unchanged.

Compare:

CONTROL:
current baseline features on the rebuilt 2020+ universe

vs.

ACD:
same samples + approved ACD/state-machine features

Do not tune architecture or hyperparameters during the first comparison.
Do not change label policy.
Do not change split logic.

The purpose is attribution: measure whether ACD features add incremental predictive value.
